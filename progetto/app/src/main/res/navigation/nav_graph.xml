<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/FirstFragment">

    <fragment
        android:id="@+id/FirstFragment"
        android:name="com.example.fillndrive.FirstFragment"
        android:label="@string/first_fragment_label"
        tools:layout="@layout/fragment_first">

        <action
            android:id="@+id/action_FirstFragment_to_MapsActivity"
            app:destination="@id/MapsActivity" />
    </fragment>

    <!--
    <fragment
        android:id="@+id/SecondFragment"
        android:name="com.example.fillndrive.SecondFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_second">

        <action
            android:id="@+id/action_SecondFragment_to_Map"
            app:destination="@id/MapsActivity" />
    </fragment>
    -->

    <activity
        android:id="@+id/MapsActivity"
        android:name="com.example.fillndrive.MapsActivity"
        android:label="@string/maps_activity_label"
        tools:layout="@layout/activity_maps" />
</navigation>